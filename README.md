fb-profile-extension
====================

Displays a "little more" information about Facebook profiles

## Usage
1. Clone the repository
2. Install the app
  ```
  npm install
  ```

3. Run run run!
  ```
  node index.js
  ```

4. Enter to http://localhost:3000
